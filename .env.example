# Himmat currently runs with local deterministic analysis and requires no secrets.
# Add provider keys here only if you introduce a server-side AI integration later.

# GEMINI_API_KEY=
# OPENAI_API_KEY=
